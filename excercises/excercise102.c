#include <stdio.h>

#define MAXLIMIT 1000;

int nextChar(void);

main() {
    int i, lim;

    lim = MAXLIMIT;

    char s[lim]

    for (i = 0; i < lim - 1 && (c = getchar()) != '\n' && c != EOF; ++i) {
        s[i] = c;
    }
}